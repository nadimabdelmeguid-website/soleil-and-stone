{
  "name": "soleil-and-stone",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run sync-cms && astro dev",
    "start": "npm run sync-cms && astro dev",
    "sync-events": "tsx scripts/sync-events.ts",
    "build": "npm run sync-cms && astro build",
    "preview": "astro preview",
    "sync-cms": "tsx scripts/sync-cms.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.0.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
